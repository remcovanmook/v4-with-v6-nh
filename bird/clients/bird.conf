# Auto-generated bird.conf

# Router ID (unique per device)
define ROUTER_ID = 198.51.100.42;

# BGP MD5 authentication password
define BGP_PASSWORD = "13456789abcdef1234567890abcdef";

# ASN offset added to base ASN 4200100000
define ASN_OFFSET = 42;

include "bird-injector-client.conf"